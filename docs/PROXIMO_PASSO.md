# Próximo passo único

Subir este pacote no repositório GitHub `rbekdrive-eng/rbek-crm`.

Não crie tabelas manualmente no Railway. O Laravel fará isso pelas migrations.

Depois do upload, conecte o repositório ao Railway. Primeiro publique o frontend; em seguida, publique o backend conectado ao PostgreSQL.
