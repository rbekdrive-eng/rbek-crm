'use client';
import Link from 'next/link'; import {usePathname,useRouter} from 'next/navigation'; import {useEffect,useState} from 'react'; import {api,clearToken,getToken} from '@/lib/api';
const nav=[['/dashboard','Visão Geral','⌂'],['/empresas','Empresas','▦'],['/oportunidades','Oportunidades','◎'],['/pipeline','Pipeline','⇥'],['/contatos','Contatos','♙'],['/atividades','Atividades','✓'],['/obras','Obras','▰'],['/licitacoes','Licitações','▤'],['/evidencias','Evidências','◈'],['/agenda','Agenda','□'],['/configuracoes','Configurações','⚙']];
export default function AppShell({children}:{children:React.ReactNode}){const path=usePathname();const router=useRouter();const [ready,setReady]=useState(false);const [name,setName]=useState('Fernando');useEffect(()=>{if(path==='/login'){setReady(true);return;}if(!getToken()){router.replace('/login');return;}api<any>('/me').then(u=>{setName(u.name||'Fernando');setReady(true)}).catch(()=>{clearToken();router.replace('/login')});},[path,router]);if(!ready&&path!=='/login')return <div className="loading-screen">Carregando plataforma…</div>;return <div className="shell"><aside className="sidebar"><div className="brand"><div className="brandmark">R.</div><div><b>R.BEK</b><small>Intelligence Platform</small></div></div><nav className="nav">{nav.map(([href,label,icon])=><Link className={path===href?'active':''} href={href} key={href}><i>{icon}</i><span>{label}</span></Link>)}</nav><div className="sidebar-footer"><div className="status-dot"/> Sistema operacional</div></aside><main className="main"><header className="topbar"><div className="global-search">⌕ <input placeholder="Buscar empresas, oportunidades, obras…"/></div><div className="user"><button className="icon-btn">♧</button><div className="avatar">{name.charAt(0)}</div><div className="user-meta"><b>{name}</b><small>Administrador</small></div><button className="logout" onClick={()=>{clearToken();router.push('/login')}}>Sair</button></div></header><div className="content">{children}</div></main></div>}
