'use client';
import {FormEvent,useState} from 'react';import {useRouter} from 'next/navigation';import {api,setToken} from '@/lib/api';
export default function Login(){const router=useRouter();const [email,setEmail]=useState('admin@rbek.eng.br');const [password,setPassword]=useState('Rbek@2026');const [error,setError]=useState('');const [loading,setLoading]=useState(false);async function submit(e:FormEvent){e.preventDefault();setLoading(true);setError('');try{const r=await api<{token:string}>('/login',{method:'POST',body:JSON.stringify({email,password})});setToken(r.token);router.push('/dashboard')}catch(e:any){setError(e.message)}finally{setLoading(false)}}return <div className="login-page"><section className="login-hero"><div className="login-brand"><div className="brandmark large">R.</div><div><h1>R.BEK</h1><p>Intelligence Platform</p></div></div><div className="hero-copy"><span>Corporate Intelligence</span><h2>Informação certa.<br/>Decisão mais rápida.<br/>Expansão com inteligência.</h2><p>Centralize empresas, obras, licitações, decisores, evidências e oportunidades em uma plataforma proprietária.</p></div><div className="hero-foot">Brasil ↔ Paraguai · Engenharia & Infraestrutura</div></section><section className="login-panel"><form className="login-card" onSubmit={submit}><div><span className="eyebrow">ACESSO SEGURO</span><h2>Bem-vindo de volta</h2><p>Entre para acessar o radar comercial da R.BEK.</p></div>{error&&<div className="alert-error">{error}</div>}<label>E-mail<input type="email" value={email} onChange={e=>setEmail(e.target.value)} required/></label><label>Senha<input type="password" value={password} onChange={e=>setPassword(e.target.value)} required/></label><button className="btn primary full" disabled={loading}>{loading?'Entrando…':'Entrar na plataforma'}</button><small className="login-help">Primeiro acesso: admin@rbek.eng.br / Rbek@2026 — altere a senha após entrar.</small></form></section></div>}
