'use client';import AppShell from '@/components/AppShell';import {api} from '@/lib/api';import {useEffect,useState} from 'react';import {Activity} from '@/lib/types';import {Loading} from '@/components/State';
export default function Agenda(){const [items,setItems]=useState<Activity[]>([]);const [loading,setLoading]=useState(true);useEffect(()=>{api<{data:Activity[]}>('/activities?per_page=300').then(r=>setItems(r.data)).finally(()=>setLoading(false))},[]);return <AppShell><div className="title-row"><div><span className="eyebrow">FOLLOW-UP</span><h1>Agenda comercial</h1><p>Compromissos, contatos e ações de prospecção.</p></div></div>{loading?<Loading/>:<div className="agenda-grid">{[0,1,2,3,4,5,6].map(offset=>{const d=new Date();d.setDate(d.getDate()+offset);const date=d.toISOString().slice(0,10);const day=items.filter(a=>a.scheduled_at?.slice(0,10)===date);return <div className="card day-card" key={date}><div className="day-head"><span>{d.toLocaleDateString('pt-BR',{weekday:'short'})}</span><b>{d.getDate()}</b></div>{day.length?day.map(a=><div className="agenda-event" key={a.id}><b>{a.type}</b><span>{a.description}</span><small>{a.company?.trade_name}</small></div>):<small className="muted">Sem atividades</small>}</div>})}</div>}</AppShell>}
